import LogoutButton from "../components/LogoutButton";

function Lobby() {
    return (
        <div>
            <h1>Lobby</h1>
            <LogoutButton />
        </div>
    );
}

export default Lobby